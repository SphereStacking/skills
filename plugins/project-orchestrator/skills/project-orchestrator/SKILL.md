---
name: project-orchestrator
description: >
  マルチフェーズプロジェクトの計画・実行・レビューオーケストレーター。
  メインセッションはプロジェクトマネージャーとして動作し、実装は
  project-orchestrator:implementer エージェントに委譲する。
  3つの並列エージェントによるレビューサイクルと Go/No-Go ゲートを実施する。
  使用場面: 「フェーズに分けて計画して」「マルチフェーズで進めたい」
  「段階的な実行計画を作成」「フェーズ N を実行」「次のフェーズを実行」
  「レビュー結果を確認」「マイグレーション/リファクタ/新規構築を段階的に管理」
  「Go/No-Go ゲートを設定」「プロジェクトを続行」「フェーズを再開」
  など、エージェント委譲によるフェーズ実行が必要なマルチステッププロジェクト全般。
  技術非依存。マイグレーション、リファクタ、新規構築、インフラ変更、
  ドキュメント整備に対応。
---

# プロジェクトオーケストレーター

`/orchestrate` コマンドで開始。2つのモード:

- **PLAN**: `/orchestrate Laravel から Nuxt へ移行` — フェーズドキュメントを生成
- **EXECUTE**: `/orchestrate execute phase 0` or `/orchestrate continue` — フェーズを実行

詳細は `/orchestrate` コマンドを参照。

## 主要コンセプト

- **マネージャーパターン**: メインセッションは計画・委譲のみ。絶対に自分で実装しない
- **実装者エージェント**: `project-orchestrator:implementer` — 動的プロンプトで任意の技術スタックに適応
- **ペルソナ**: 堅実型/防御型/適応型/検証型 — フェーズの性質に応じてマネージャーが選択・注入
- **レビューサイクル**: 3エージェント並列（コードレビュー、専門家、批判的）
- **トリアージポリシー**: Critical/High → 修正、Medium → 評価、Low → 破棄
- **Go/No-Go ゲート**: 次フェーズへ進む前にユーザー承認必須
- **セッション継続**: ディスク上のフェーズドキュメントがセッションをまたいで永続化
